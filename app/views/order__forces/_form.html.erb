<%= form_with(model: order__force, local: true) do |form| %>
  <% if order__force.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(order__force.errors.count, "error") %> prohibited this order__force from being saved:</h2>

      <ul>
      <% order__force.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :num %>
    <%= form.text_field :num %>
  </div>

  <div class="field">
    <%= form.label :hz %>
    <%= form.number_field :hz %>
  </div>

  <div class="field">
    <%= form.label :client %>
    <%= form.text_field :client %>
  </div>

  <div class="field">
    <%= form.label :date %>
    <%= form.text_field :date %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
